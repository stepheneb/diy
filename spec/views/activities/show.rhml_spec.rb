require 'spec_helper'

describe "/activities/show.rhtml" do
  # this generated block is not working, because there is something wrong with
  # the current version mixture of rspec / rails / for the DIY project.
  # TODO: Talk to aaron, others about upgrading?
  #
  # before(:each) do
  #   assigns[:activity] = @activity = stub_model(Activity)
  # end
  # 
  # it "renders attributes in <p>" do
  #   render
  # end
end
