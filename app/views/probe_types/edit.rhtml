<h1>Editing probe_type</h1>

<%= error_messages_for :probe_type %>

<% form_for(:probe_type, :url => probe_type_path(@probe_type), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', probe_type_path(@probe_type) %> |
<%= link_to 'Back', probe_types_path %>